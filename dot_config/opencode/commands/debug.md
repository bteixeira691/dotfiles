---
name: debug
description: Debug a bug (uses the debug skill)
skill: debug
---

# /debug — debug a bug or unexpected behavior

Use the `debug` skill. The agent will:
1. Reproduce the issue
2. Localize the root cause
3. Form and test hypotheses
4. Fix the bug with a test
