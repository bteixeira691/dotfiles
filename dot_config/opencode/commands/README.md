# OpenCode commands directory
# Add custom slash commands here. Each .md file becomes a /command in OpenCode.

# Built-in commands (from config.json):
#   /commit     — git commit with AI message
#   /explain    — explain selected code
#   /review     — review current changes
#   /test       — generate tests

# Add more by creating .md files:
#   /plan       — switch to plan agent
#   /refactor   — switch to refactor skill
#   /debug      — switch to debug skill
#   /dotfiles   — switch to dotfiles skill
