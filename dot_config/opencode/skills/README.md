# OpenCode skills directory
# Drop .md files in here and OpenCode will read them on demand.
# A "skill" is a reusable playbook for a specific domain or task.

# File naming: use kebab-case, e.g. "refactor-python.md"
# OpenCode loads skill files into the system prompt when their
# keywords match the user's request.
